window.WRITEUPS = [
  {"body": "✦ Documented by Claude · 2026-08-14 Write-up Kind Claude Code plugin Author Christopher Whitelam Status Shipped Surface /write-up Documented 2026-08-18 Write-up is a skill for Claude Code that writes the story of a finished piece of work as a self-contained HTML article, and accumulates those articles into a small local wiki. It exists because a git history records what changed and never records why, and because the reasoning that produced a change is at its most complete in the minutes after the work is done and gone a week later. This article is itself a write-up, generated by the tool it describes. Why A commit preserves a diff and a one-line message. A pull request adds a title and whatever the author had the patience to type into the description box. Neither preserves the three approaches that were tried and abandoned, the constraint that made the obvious design impossible, or the one function that turned out to be the whole trick. That context lives in a chat transcript nobody will ever reopen. The gap is worst exactly where it hurts most: six months later, when someone (often the original author) asks why a thing was built this way and the honest answer is that nobody remembers. The goal here was to capture that reasoning at the moment it is still cheap to capture, in a format someone would actually read. What A Claude Code skill that writes <outputDir>/<branch>.html from what the model already holds in session context, in six fixed sections: Why, What, How, Decisions, Day by day, Highlights. Decisions is an Architecture Decision Record log, so the choices that shaped the work stay scannable without rereading the whole article. A generated front page with full-text search, status filter chips, and hover previews of every article. Automatic \"Referenced by\" backlinks derived from a manifest, so the wiki cross-links itself without hand maintenance. Glossary hovercards for non-obvious terms, from a curated dictionary rather than runtime guessing. A gated Stop hook that asks for the article exactly once, at genuine wrap-up, and a day-journal entry once per working day. A POSIX sh Stop hook with no dependencies at all, and an optional Python CLI ( init , build , digest , suggest , shot , sweep , doctor ) for the mechanical jobs. Python is never required. Installed as a plugin in two commands, which wires the hook without anyone editing settings.json . A home wiki at ~/.claude/write-ups/ for work that is not a branch and may not be code: research that reached an answer, a decision argued out over one session. How Started from the observation that the model already knows the story at the end of a session. No mining, no summarisation pass: chose \"write from live context\" over \"reconstruct from the transcript\" because reconstruction is expensive and produces a worse story than the one already in working memory. [1] Fixed the article structure instead of letting each one find its own shape. Six sections, always in the same order, so a reader who has seen one article can skim any other. Chose a rigid skeleton over freeform prose because consistency is what makes a wiki scannable. Made the page a single self-contained HTML file: styles inline, no build step, no framework, no network. Chose static files over a documentation site generator because a wiki nobody has to install is a wiki that still opens in three years. Wrote the Highlights section as verbatim code rather than prose descriptions of code. A snippet you can copy is worth a paragraph explaining it, and it cannot drift into being wrong the way a summary can. Generated the index rather than hand-maintaining it. An early version had the model append an entry per article; entries went missing for days because a hand-maintained list is a hand-forgotten list. Chose \"scan the folder and rebuild\" over \"append on write\". [2] Gated the Stop hook hard. It fires only when the tree is clean, the branch is ahead of the default branch, and the work is pushed, and then only if the article is missing or older than the newest commit. Chose a narrow gate over a reminder-on-every-turn because a hook that cries wolf gets disabled within a day. [3] Extracted the forge-specific URLs into .write-up.json , detected from the git remote at init time. GitHub, GitLab, Bitbucket, and Azure DevOps each get their own link patterns; an unrecognised host resolves to no links at all rather than broken ones. Decisions Write from session context, never from the transcript Aug 13 Re-reading a transcript costs tokens and yields a flatter story than the one the model is already holding. The digest tool exists only for work that genuinely spanned sessions. The index is generated, never hand-edited Aug 13 A hand-maintained list silently rots. Scanning the article folder makes the front page structurally incapable of missing an article. One Python CLI instead of six scripts Aug 14 Six scripts across two languages meant six paths to resolve and two runtimes to install. One entry point, standard library only, works identically on every platform the hook runs on. Screenshots are the exception, and never redacted Aug 14 For most work the code snippet is the illustration. Blurring real data was tried and rejected: it reads as a leaked document and advertises that something sensitive is behind it. Runtime JS ships with the skill Aug 14 The first extraction attempt referenced two script files that lived only in the author's wiki folder, so a fresh install rendered with two silent 404s. The installer now carries them and build restores any that go missing. The hook is POSIX sh, and Python is optional Aug 18 Supersedes the one-CLI decision above for the hook specifically. Claude Code runs hook commands through sh -c on macOS and Linux and Git Bash on Windows, so a shell script needs nothing installed. Python is not in that position: macOS has shipped no bundled Python 3 since 12.3 and Windows ships none. The CLI survives for the mechanical jobs, with a documented fallback for every command. Ship as a plugin, not a skill plus instructions Aug 18 Installing meant cloning, running a script, then hand-pasting JSON into settings.json . Four manual steps, one of them editing a config people care about, which is where they quit. A plugin carries the skill and the hook together, so it is two commands and no config editing. A home wiki for work that is not a branch Aug 18 The tool was hard-gated on git: init refused outside a repo and the hook was permanently silent there. But a lot of real work is research, a decision, an investigation. The wiki now falls back to ~/.claude/write-ups/ . The hook stays repo-only on purpose, because branch state is the only honest \"this is finished\" signal and a hook that guessed would nag. Commit to one presentation, drop the Old/New toggle Aug 18 The toggle let a reader turn off motion and hide Decisions and Day by day. Keeping both modes meant every CSS rule carried a body.new qualifier and two states to reason about, to serve a choice nobody was making. Removed: 48 lines out of the runtime. Day by day Wednesday · Aug 13 Audit Inventoried what the private version actually depended on: three files in the skill folder, four more outside it, all of the latter gitignored. Found the real blocker: the article template loads write-up-ui.js and glossary.js by relative path, and neither shipped with the skill. Counted five hardcoded forge URLs across the template and the hook. Thursday · Aug 14 Extraction Collapsed two PowerShell scripts and two Python scripts into one cross-platform CLI. Added forge detection so init reads the git remote and writes the right link patterns without being told. Ported the Stop hook from PowerShell to Python, keeping the gate logic identical. Made build self-healing: it restores any runtime asset missing from the wiki folder. Monday · Aug 17 Named it, then proved it Renamed from Blueprint to Write-up. Codex found the killer fact: backstory-cli already ships on PyPI mining Claude Code sessions for e", "date": "2026-08-18", "lead": "Write-up is a skill for Claude Code that writes the story of a finished piece of work as a self-contained HTML article, and accumulates those articles into a small local wiki. It exists because a git history records what changed and never records why, and because the reasoning that produced a change is at its most complete in the minutes after the work is done and gone a week later. This article is itself a write-up, generated by the tool it describes.", "refs": [], "slug": "write-up", "status": "Shipped", "ticket": "", "title": "Write-up"}
];
